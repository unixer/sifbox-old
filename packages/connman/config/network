# Network configuration

# "LAN" or "WLAN" or "" (default is "" <--> "LAN")
NETWORK="LAN"

# Network interface name or address (e.g. "eth0" or "00:0d:56:ec:ba:0c")
IFACE=""

# Static IP address ("" for DHCP) in CIDR format (e.g. "192.168.1.1/24")
ADDRESS=""

# Network gateway (e.g. "192.168.1.254")
GATEWAY=""

# List of DNS servers (e.g. "192.168.1.253;192.168.1.254")
DNS_SERVER=""

SSID=""

# "true" or ""
HIDDEN=""

# "WEP" or "PSK" or "" or "RSN"
SECURITY=""

PASSPHRASE=""

# Services to be started at boot time
TELNET_SERVER="no"
FTP_SERVER="no"
HTTP_SERVER="no"
SAMBA_SERVER="no"

# Use Zeroconf service discovery
ZEROCONF="yes"

# Use UPnP MediaServer devices auto-discovery

UPNP="no"

# Network Stream

ICECAST="no"
SHOUTCAST="no"
# WARNING: SHOUTcast TV may contain streams with adult content!
SHOUTCASTTV="no"
WHITELIST=""
BLACKLIST="adult porn xxx ESS SWCTV SWPTV Subscription"
TIMEOUT=10
TRIES=10

# Generic shares settings

NET_RESCAN_DELAY=180 # Set to 0 to disable network rescan, or else time (secs)
                     # between scans.

# Samba settings

# Note that configured Samba shares cannot contain spaces in their names.
SMB_USER=""           # User Login ("" for none)
SMB_PWD=""                 # User Password ("" for none)

# Static Samba shares can be defined as shown in the commented examples below, 
# where <%>,<@>,<&>,<#> field separators must be present as shown (Note that 
# the password cannot contain  these special delimiters):

#STATIC_SMB="username<%>password<@>ip<&>myservername<#>share1<#>share2<#>share3"
#STATIC_SMB="<%><@>ip<&>myservername<#>share1"

#En principio el geexbox deberíreconocer al arrancar las unidades compartidas pero por si acaso pruebo a definirla de forma estáca con la ip donde tengo el servidor de archivos el nombre y la unidad
#STATIC_SMB="<%><@>192.168.1.4<&>SERVIDOR<#>peliculas" 


